# Nixpacks config for Railway
[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci --prefer-offline --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "sh -c 'if [ -n \"$DATABASE_URL\" ]; then npx prisma migrate deploy; fi && npm run start'"
