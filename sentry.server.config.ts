// Sentry server init is now done inline in instrumentation.ts
// (not via this file). Left as an empty module to satisfy any
// legacy imports; can be deleted once we confirm no build tool
// auto-picks it up.
export {};
